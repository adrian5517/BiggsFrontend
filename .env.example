# Example environment for the frontend (copy to .env.local for local dev)

# API backend URL
NEXT_PUBLIC_API_URL=http://localhost:5000

# Optional: Postgres-backed POS fallback settings used by backend
# These are read by the backend service; front-end uses the API only.
# - `POS_LIST_FALLBACK_FILES` can be a JSON array of fully-qualified URLs
#   or filenames. If filenames are provided, the backend will use
#   `POS_FILE_URL_TEMPLATE` to construct download URLs where `{filename}`
#   is replaced with the filename.
# Example (fully-qualified URL array):
POS_LIST_FALLBACK_FILES=["https://example.com/sample.csv"]
# Example (use filename + template):
# POS_LIST_FALLBACK_FILES=["sample.csv"]
# POS_FILE_URL_TEMPLATE=https://biggsph.com/pos_files/{filename}

# Debug helper (backend):
# POS_DEBUG=true
